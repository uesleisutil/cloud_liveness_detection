name: Deploy to AWS EC2

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Install AWS CLI
        run: |
          sudo apt-get update
          sudo apt-get install -y awscli

      - name: Ensure SSH Access in Security Group
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: ${{ secrets.AWS_DEFAULT_REGION }}
          SECURITY_GROUP_ID: ${{ secrets.SECURITY_GROUP_ID }}
        run: |
          set -x
          RULE_EXISTS=$(aws ec2 describe-security-groups --group-ids $SECURITY_GROUP_ID --query "SecurityGroups[0].IpPermissions[?FromPort==\`22\` && ToPort==\`22\` && IpProtocol=='tcp' && contains(IpRanges[].CidrIp, '0.0.0.0/0')]" --output text)
          if [ -z "$RULE_EXISTS" ]; then
            aws ec2 authorize-security-group-ingress --group-id $SECURITY_GROUP_ID --protocol tcp --port 22 --cidr 0.0.0.0/0
          else
            echo "A regra de segurança para conexões SSH na porta 22 já existe."
          fi
          
          # Check outbound rule for SSH
          OUTBOUND_RULE_EXISTS=$(aws ec2 describe-security-groups --group-ids $SECURITY_GROUP_ID --query "SecurityGroups[0].IpPermissionsEgress[?IpProtocol=='tcp' && FromPort==\`22\` && ToPort==\`22\` && contains(IpRanges[].CidrIp, '0.0.0.0/0')]" --output text)
          if [ -z "$OUTBOUND_RULE_EXISTS" ]; then
            aws ec2 authorize-security-group-egress --group-id $SECURITY_GROUP_ID --protocol tcp --port 22 --cidr 0.0.0.0/0
          else
            echo "A regra de segurança de saída para conexões SSH na porta 22 já existe."
          fi

      - name: Ensure Subnet and Route Table Configuration
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: ${{ secrets.AWS_DEFAULT_REGION }}
          SUBNET_ID: ${{ secrets.SUBNET_ID }}
          INTERNET_GATEWAY_ID: ${{ secrets.INTERNET_GATEWAY_ID }}
        run: |
          set -x
          ROUTE_TABLE_ID=$(aws ec2 describe-route-tables --filters "Name=association.subnet-id,Values=$SUBNET_ID" --query "RouteTables[0].RouteTableId" --output text)
          IGW_ROUTE_EXISTS=$(aws ec2 describe-route-tables --route-table-ids $ROUTE_TABLE_ID --query "RouteTables[0].Routes[?GatewayId=='igw-*']" --output text)
          if [ -z "$IGW_ROUTE_EXISTS" ]; then
            echo "A Subnet não está associada a uma tabela de rotas com um Gateway da Internet. Adicionando rota."
            aws ec2 create-route --route-table-id $ROUTE_TABLE_ID --destination-cidr-block 0.0.0.0/0 --gateway-id $INTERNET_GATEWAY_ID
          else
            echo "A Subnet está corretamente configurada."
          fi

          VPC_ID=$(aws ec2 describe-subnets --subnet-ids $SUBNET_ID --query "Subnets[0].VpcId" --output text)

          DNS_RESOLUTION=$(aws ec2 describe-vpcs --vpc-ids $VPC_ID --query "Vpcs[0].EnableDnsSupport" --output text)
          if [ "$DNS_RESOLUTION" != "true" ]; then
            echo "Ativando resolução de DNS para a VPC."
            aws ec2 modify-vpc-attribute --vpc-id $VPC_ID --enable-dns-support
          else
            echo "Resolução de DNS já está ativada."
          fi

          DNS_HOSTNAMES=$(aws ec2 describe-vpcs --vpc-ids $VPC_ID --query "Vpcs[0].EnableDnsHostnames" --output text)
          if [ "$DNS_HOSTNAMES" != "true" ]; then
            echo "Ativando hostnames DNS para a VPC."
            aws ec2 modify-vpc-attribute --vpc-id $VPC_ID --enable-dns-hostnames
          else
            echo "Hostnames DNS já estão ativados."
          fi

          # Ensure outbound access for DNS and HTTP/HTTPS
          OUTBOUND_DNS_RULE=$(aws ec2 describe-security-groups --group-ids $SECURITY_GROUP_ID --query "SecurityGroups[0].IpPermissionsEgress[?IpProtocol=='udp' && FromPort==\`53\` && ToPort==\`53\` && contains(IpRanges[].CidrIp, '0.0.0.0/0')]" --output text)
          if [ -z "$OUTBOUND_DNS_RULE" ]; then
            aws ec2 authorize-security-group-egress --group-id $SECURITY_GROUP_ID --protocol udp --port 53 --cidr 0.0.0.0/0
          else
            echo "Regra de segurança de saída para DNS já existe."
          fi

          OUTBOUND_HTTP_RULE=$(aws ec2 describe-security-groups --group-ids $SECURITY_GROUP_ID --query "SecurityGroups[0].IpPermissionsEgress[?IpProtocol=='tcp' && (FromPort==\`80\` || FromPort==\`443\`) && contains(IpRanges[].CidrIp, '0.0.0.0/0')]" --output text)
          if [ -z "$OUTBOUND_HTTP_RULE" ]; then
            aws ec2 authorize-security-group-egress --group-id $SECURITY_GROUP_ID --protocol tcp --port 80 --cidr 0.0.0.0/0
            aws ec2 authorize-security-group-egress --group-id $SECURITY_GROUP_ID --protocol tcp --port 443 --cidr 0.0.0.0/0
          else
            echo "Regra de segurança de saída para HTTP/HTTPS já existe."
          fi

      - name: Check Outbound Rules in Security Group
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: ${{ secrets.AWS_DEFAULT_REGION }}
          SECURITY_GROUP_ID: ${{ secrets.SECURITY_GROUP_ID }}
        run: |
          set -x
          OUTBOUND_RULE_EXISTS=$(aws ec2 describe-security-groups --group-ids $SECURITY_GROUP_ID --query "SecurityGroups[0].IpPermissionsEgress[?IpProtocol=='-1' && contains(IpRanges[].CidrIp, '0.0.0.0/0')]" --output text)
          if [ -z "$OUTBOUND_RULE_EXISTS" ]; then
            echo "Adicionando regra de saída para permitir todas as conexões de saída."
            aws ec2 authorize-security-group-egress --group-id $SECURITY_GROUP_ID --protocol -1 --port -1 --cidr 0.0.0.0/0
          else
            echo "Regras de saída já configuradas."
          fi

      - name: Check Network ACL Configuration
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: ${{ secrets.AWS_DEFAULT_REGION }}
          SUBNET_ID: ${{ secrets.SUBNET_ID }}
        run: |
          set -x
          NETWORK_ACL_ID=$(aws ec2 describe-network-acls --filters "Name=association.subnet-id,Values=$SUBNET_ID" --query "NetworkAcls[0].NetworkAclId" --output text)
          EXISTING_RULES=$(aws ec2 describe-network-acls --network-acl-ids $NETWORK_ACL_ID --query "NetworkAcls[0].Entries[?RuleNumber==\`100\` || RuleNumber==\`110\`].RuleNumber" --output text)
          
          if [[ "$EXISTING_RULES" == *"100"* || "$EXISTING_RULES" == *"110"* ]]; then
            if [[ "$EXISTING_RULES" == *"100"* ]]; then
              echo "Deletando a regra existente 100 na ACL de rede."
              aws ec2 delete-network-acl-entry --network-acl-id $NETWORK_ACL_ID --rule-number 100 --ingress
            fi
            if [[ "$EXISTING_RULES" == *"110"* ]]; then
              echo "Deletando a regra existente 110 na ACL de rede."
              aws ec2 delete-network-acl-entry --network-acl-id $NETWORK_ACL_ID --rule-number 110 --ingress
            fi
          fi

          echo "Adicionando regra de entrada 100 na ACL de rede para permitir conexões SSH."
          aws ec2 create-network-acl-entry --network-acl-id $NETWORK_ACL_ID --rule-number 100 --protocol tcp --port-range From=22,To=22 --ingress --rule-action allow --cidr-block 0.0.0.0/0

          echo "Adicionando regra de entrada 110 na ACL de rede para permitir conexões SSH."
          aws ec2 create-network-acl-entry --network-acl-id $NETWORK_ACL_ID --rule-number 110 --protocol tcp --port-range From=22,To=22 --ingress --rule-action allow --cidr-block 0.0.0.0/0

          OUTBOUND_ACL_RULE_EXISTS=$(aws ec2 describe-network-acls --network-acl-ids $NETWORK_ACL_ID --query "NetworkAcls[0].Entries[?RuleNumber==\`111\` && RuleAction=='allow' && Protocol=='6' && Egress==true]" --output text)
          if [ -z "$OUTBOUND_ACL_RULE_EXISTS" ]; then
            echo "Adicionando regra de saída na ACL de rede para permitir todas as conexões de saída."
            aws ec2 create-network-acl-entry --network-acl-id $NETWORK_ACL_ID --rule-number 111 --protocol tcp --port-range From=0,To=65535 --egress --rule-action allow --cidr-block 0.0.0.0/0
          else
            echo "Regras de saída da ACL de rede já configuradas."
          fi

      - name: Create EC2 Instance
        id: create_ec2
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: ${{ secrets.AWS_DEFAULT_REGION }}
          EC2_KEY_NAME: ${{ secrets.EC2_KEY_NAME }}
          SECURITY_GROUP_ID: ${{ secrets.SECURITY_GROUP_ID }}
          SUBNET_ID: ${{ secrets.SUBNET_ID }}
          AMI_ID: ${{ secrets.AMI_ID_AWS }}
        run: |
          set -x
          INSTANCE_ID=$(aws ec2 run-instances --image-id $AMI_ID --count 1 --instance-type t2.micro --key-name $EC2_KEY_NAME --security-group-ids $SECURITY_GROUP_ID --subnet-id $SUBNET_ID --associate-public-ip-address --user-data file://ec2-user-data.sh --query 'Instances[0].InstanceId' --output text)
          echo "INSTANCE_ID=$INSTANCE_ID" >> $GITHUB_ENV
          aws ec2 wait instance-running --instance-ids $INSTANCE_ID

          # Ensure outbound access for the instance
          INSTANCE_SECURITY_GROUP=$(aws ec2 describe-instances --instance-ids $INSTANCE_ID --query "Reservations[0].Instances[0].SecurityGroups[0].GroupId" --output text)
          OUTBOUND_RULE_EXISTS=$(aws ec2 describe-security-groups --group-ids $INSTANCE_SECURITY_GROUP --query "SecurityGroups[0].IpPermissionsEgress[?IpProtocol=='-1' && contains(IpRanges[].CidrIp, '0.0.0.0/0')]" --output text)
          if [ -z "$OUTBOUND_RULE_EXISTS" ]; then
            aws ec2 authorize-security-group-egress --group-id $INSTANCE_SECURITY_GROUP --protocol -1 --port -1 --cidr 0.0.0.0/0
          else
            echo "Regras de saída já configuradas."
          fi

      - name: Get instance IP
        id: get_instance_ip
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: ${{ secrets.AWS_DEFAULT_REGION }}
          INSTANCE_ID: ${{ env.INSTANCE_ID }}
        run: |
          set -x
          INSTANCE_IP=$(aws ec2 describe-instances --instance-ids $INSTANCE_ID --query 'Reservations[0].Instances[0].PublicIpAddress' --output text)
          if [ -z "$INSTANCE_IP" ]; then
            echo "Failed to retrieve instance IP"
            exit 1
          fi
          echo "INSTANCE_IP=$INSTANCE_IP" >> $GITHUB_ENV

      - name: Verify SSH Connectivity
        env:
          INSTANCE_IP: ${{ env.INSTANCE_IP }}
        run: |
          echo "Verificando conectividade SSH para $INSTANCE_IP na porta 22"
          for i in {1..10}; do
            nc -zv $INSTANCE_IP 22 && break
            echo "Tentativa $i: Falha ao conectar na porta 22. Aguardando 10 segundos antes de tentar novamente."
            sleep 10
          done

      - name: Deploy Streamlit App
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: ${{ secrets.AWS_DEFAULT_REGION }}
          EC2_KEY_NAME: ${{ secrets.EC2_KEY_NAME }}
          SECURITY_GROUP_ID: ${{ secrets.SECURITY_GROUP_ID }}
          SUBNET_ID: ${{ secrets.SUBNET_ID }}
          AMI_ID: ${{ secrets.AMI_ID_AWS }}
          EC2_KEY_PEM: ${{ secrets.EC2_KEY_PEM }}
          INSTANCE_IP: ${{ env.INSTANCE_IP }}
        run: |
          echo "${{ secrets.EC2_KEY_PEM }}" > ec2_key.pem
          chmod 600 ec2_key.pem

          # Verifique se a regra de saída para HTTP/HTTPS existe
          OUTBOUND_HTTP_RULE=$(aws ec2 describe-security-groups --group-ids $SECURITY_GROUP_ID --query "SecurityGroups[0].IpPermissionsEgress[?IpProtocol=='tcp' && (FromPort==\`80\` || FromPort==\`443\`) && contains(IpRanges[].CidrIp, '0.0.0.0/0')]" --output text)
          if [ -z "$OUTBOUND_HTTP_RULE" ]; then
            aws ec2 authorize-security-group-egress --group-id $SECURITY_GROUP_ID --protocol tcp --port 80 --cidr 0.0.0.0/0
            aws ec2 authorize-security-group-egress --group-id $SECURITY_GROUP_ID --protocol tcp --port 443 --cidr 0.0.0.0/0
          else
            echo "Regras de saída para HTTP/HTTPS já existem."
          fi

          # Verifica a conectividade de rede
          echo "Verificando a conectividade de rede"
          if ping -c 4 google.com; then
            echo "Conectividade de rede está funcionando"
          else
            echo "Sem conectividade de rede. Verifique as regras de segurança e a configuração da VPC."
            exit 1
          fi

          # Crie um arquivo de script remoto
          cat <<'EOF' > deploy_script.sh
          sudo pkill -f yum || true
          sudo pkill -f apt-get || true
          sudo pkill -9 -f yum || true
          sudo pkill -9 -f apt-get || true
          sudo killall -9 yum || true
          sudo killall -9 apt-get || true

          sudo yum clean all
          sudo yum makecache

          echo "Atualizando pacotes e instalando dependências"
          timeout 5m sudo yum update -y
          timeout 5m sudo yum install -y python3 tmux
          
          echo "Instalando bibliotecas Python"
          sudo pip3 install --upgrade pip
          sudo pip3 install -r /home/ec2-user/liveness_detection/requirements.txt

          # Verifica se o Streamlit foi instalado corretamente
          if ! command -v streamlit &> /dev/null
          then
              echo "Streamlit não foi instalado corretamente"
              exit 1
          fi

          # Inicia o Streamlit usando tmux
          tmux new -d -s streamlit_session "streamlit run /home/ec2-user/liveness_detection/app/streamlit_app.py --server.port 8080 --server.address 0.0.0.0"
          EOF

          # Transfira o arquivo de script para a instância EC2
          scp -o StrictHostKeyChecking=no -i ec2_key.pem deploy_script.sh ec2-user@$INSTANCE_IP:/home/ec2-user/deploy_script.sh

          # Crie o diretório liveness_detection na instância EC2
          ssh -o StrictHostKeyChecking=no -i ec2_key.pem ec2-user@$INSTANCE_IP "mkdir -p /home/ec2-user/liveness_detection"

          # Transfira o código da aplicação para a instância EC2
          scp -o StrictHostKeyChecking=no -i ec2_key.pem -r * ec2-user@$INSTANCE_IP:/home/ec2-user/liveness_detection

          # Execute o script na instância EC2
          ssh -o StrictHostKeyChecking=no -i ec2_key.pem ec2-user@$INSTANCE_IP "chmod +x /home/ec2-user/deploy_script.sh && /home/ec2-user/deploy_script.sh"

          # Adicione uma regra de entrada para permitir o tráfego HTTP na porta 8080
          aws ec2 authorize-security-group-ingress --group-id $SECURITY_GROUP_ID --protocol tcp --port 8080 --cidr 0.0.0.0/0

